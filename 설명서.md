# InSIGHT 상세 설치 가이드

> 처음 세팅하는 팀원을 위한 단계별 안내서입니다.

---

## 목차

1. [사전 준비](#1-사전-준비)
2. [conda 환경 설치](#2-conda-환경-설치)
3. [.env 파일 설정](#3-env-파일-설정)
4. [Gemini API Key 발급](#4-gemini-api-key-발급)
5. [Vertex AI 서비스 계정 설정](#5-vertex-ai-서비스-계정-설정)
6. [Instagram 세션 설정](#6-instagram-세션-설정)
7. [앱 실행 및 확인](#7-앱-실행-및-확인)
8. [자주 묻는 오류](#8-자주-묻는-오류)

---

## 1. 사전 준비

### Miniconda 설치 확인

```bash
conda --version
```

설치되어 있지 않으면 [Miniconda 다운로드](https://docs.conda.io/en/latest/miniconda.html) 에서 설치하세요.

### 저장소 클론

```bash
git clone https://github.com/YOUR_REPO/Last_Project_test.git
cd Last_Project_test
```

---

## 2. conda 환경 설치

```bash
# 환경 생성 (최초 1회, 3~5분 소요)
conda env create -f environment_app.yml

# 환경 활성화
conda activate insight

# 설치 확인
python -c "import gradio, google.genai, instaloader; print('설치 완료')"
```

> 이후 앱을 실행할 때마다 `conda activate insight` 를 먼저 실행해야 합니다.

---

## 3. .env 파일 설정

프로젝트 루트에 `.env` 파일을 생성합니다.

```bash
cp .env.example .env
```

`.env` 파일을 텍스트 편집기로 열어 아래 항목을 채워넣습니다:

```
# Gemini API Key (Stage 3 필수)
GEMINI_API_KEY=AIzaSy...

# Vertex AI 서비스 계정 JSON 경로 (Stage 2 선택)
GOOGLE_APPLICATION_CREDENTIALS=/Users/내이름/Downloads/insight-service-account.json

# Instagram 로그인 아이디 (Instagram URL 사용 시)
INSTAGRAM_USERNAME=404_humannotfound_kr
```

> `.env` 파일은 git에 올라가지 않습니다. 절대 공유하거나 커밋하지 마세요.

---

## 4. Gemini API Key 발급

**Stage 3 (AI 시각 분석)** 에 필요합니다. 무료 티어로 충분합니다.

1. [Google AI Studio](https://aistudio.google.com/app/apikey) 접속
2. Google 계정으로 로그인
3. **Create API key** 클릭
4. 생성된 `AIzaSy...` 형식의 키를 복사
5. `.env` 파일의 `GEMINI_API_KEY=` 뒤에 붙여넣기

```
GEMINI_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXX
```

**무료 한도:** Gemini 2.5 Flash 기준 분당 15회, 일 1,500회 무료

---

## 5. Vertex AI 서비스 계정 설정

**Stage 2 (공식 SynthID 탐지)** 에 필요합니다. 없으면 Stage 2는 자동 스킵됩니다.

### 5-1. GCP 프로젝트 확인

프로젝트 ID: `insight-494801` (이미 생성되어 있음)

### 5-2. 서비스 계정 생성

1. [GCP Console](https://console.cloud.google.com) 접속
2. 좌측 메뉴 → **IAM 및 관리자** → **서비스 계정**
3. **서비스 계정 만들기** 클릭
4. 이름 입력 (예: `insight-local`) → **만들기 및 계속**
5. 역할 추가:
   - **Vertex AI 사용자** 선택
6. **완료** 클릭

### 5-3. JSON 키 다운로드

1. 생성된 서비스 계정 이메일 클릭
2. **키** 탭 → **키 추가** → **새 키 만들기**
3. **JSON** 선택 → **만들기**
4. JSON 파일이 자동 다운로드됨 (예: `insight-494801-abcdef123456.json`)

### 5-4. .env 파일에 경로 입력

다운로드된 JSON 파일의 **전체 경로**를 입력합니다.

```
# Mac/Linux 예시
GOOGLE_APPLICATION_CREDENTIALS=/Users/woochul/Downloads/insight-494801-abcdef123456.json

# Windows 예시
GOOGLE_APPLICATION_CREDENTIALS=C:\Users\woochul\Downloads\insight-494801-abcdef123456.json
```

---

## 6. Instagram 세션 설정

Instagram URL 분석 기능 사용 시 필요합니다. **로컬 실행 전용** 기능입니다.

### 6-1. Instagram 아이디 설정

```
INSTAGRAM_USERNAME=404_humannotfound_kr
```

### 6-2. 세션 파일 생성 (최초 1회)

```bash
conda activate insight
instaloader --load-cookies=chrome
```

> Chrome에 Instagram이 로그인된 상태여야 합니다.
> 성공 시: `Saved session to ~/.config/instaloader/session-아이디` 메시지 출력

### 세션 만료 시

세션이 만료되면 같은 명령어로 재생성합니다:

```bash
instaloader --load-cookies=chrome
```

---

## 7. 앱 실행 및 확인

```bash
conda activate insight
python app.py
```

브라우저에서 자동으로 열리거나, `http://localhost:7860` 으로 접속합니다.

### 정상 실행 화면

```
Running on local URL:  http://127.0.0.1:7860
```

### 각 Stage 작동 확인

| Stage | 정상 작동 시 | 설정 미완료 시 |
|-------|------------|--------------|
| Stage 1 | 점수 및 분석 결과 출력 | 항상 작동 (설정 불필요) |
| Stage 2 | SynthID 신뢰도 출력 | "스킵 (Vertex AI 미연결)" 표시 |
| Stage 3 | AI 판정 및 근거 출력 | "GEMINI_API_KEY 없음" 오류 |

---

## 8. 자주 묻는 오류

### `ModuleNotFoundError: No module named 'gradio'`

conda 환경이 활성화되지 않은 상태입니다.

```bash
conda activate insight
```

---

### `GEMINI_API_KEY 없음` 오류

`.env` 파일에 API 키가 설정되지 않았습니다.

1. `.env` 파일이 프로젝트 루트에 있는지 확인
2. `GEMINI_API_KEY=AIzaSy...` 형식으로 입력되어 있는지 확인
3. 또는 앱 UI의 **Gemini API Key** 입력란에 직접 입력

---

### `Vertex AI SynthID 오류`

서비스 계정 JSON 경로가 잘못됐거나 권한이 없는 경우입니다.

1. JSON 파일이 해당 경로에 실제로 존재하는지 확인
2. GCP Console에서 서비스 계정에 **Vertex AI 사용자** 역할이 부여되어 있는지 확인
3. Stage 2 체크박스를 해제하면 스킵하고 진행 가능

---

### Instagram `401 Unauthorized` 오류

Instagram 세션이 만료됐습니다.

```bash
instaloader --load-cookies=chrome
```

재실행 후 앱을 다시 시작합니다.

---

### Windows에서 `conda activate insight` 안 됨

Anaconda Prompt 또는 Miniforge Prompt를 사용하세요. 일반 CMD/PowerShell에서는 `conda activate` 가 동작하지 않을 수 있습니다.
